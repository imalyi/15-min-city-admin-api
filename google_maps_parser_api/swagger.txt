openapi: 3.0.0
info:
  title: Sample API
  description: Optional multiline or single-line description in [CommonMark](http://commonmark.org/help/) or HTML.
  version: 0.1.9

servers:
  - url: http://maps_parser_manager_api.1213213.xyz/
    description: Production endpoint
  - url: http://maps_parser_manager_api.1213213.xyz/
    description: Test endpoint, latest build from main branch

paths:
   /client:
      get:
        summary: Returns a list of clients.
        description: Every client is docker container which is ready to parsing data
        responses:
          '200':
            description: A JSON array of clients
            content:
              application/json:
                schema:
                  type: array
                  items:
                    type: object
                    properties:
                      name:
                        type: string
                        example: node1
                      last_online:
                        type: string